# 会话中心第二版 UI

## 目标与边界

在现有管理端渐进改造布局、展示投影和低风险交互。未修改 API 协议、数据库结构、
权限、Runtime、审批或人工接管的业务规则。没有新增依赖或独立 Demo 项目。

## 本版界面

- 产品名称为「AI 客服工作台」，删除 Aira、星形标识、英文模板标题与宣传卡片。
- 左侧按工作队列与知识/AI 分组；待处理、待审批、人工接管、已解决入口复用现有筛选。
- 知识库、智能体、系统设置明确标记未开放，没有对应假页面。
- 功能导航、会话队列、上下文面板独立收起。中央区域使用弹性布局，阅读内容限宽。
- 布局状态使用 localStorage 的 `customer-workspace-layout-v2`；读取失败时仍可在本次页面内操作。
- 收起不会卸载消息或回复输入区；关闭上下文后焦点回到工具栏按钮。
- 搜索以小型提交按钮或回车触发，状态使用直接筛选链接，清除搜索同步清空输入框。
- 消息流区分客户消息、持久化 AI 动作、已发送回复、系统事件，避免左右相隔过远。
- 保留人工回复、常用回复、失败草稿和原请求快照，已解决会话清楚显示不可继续回复。

## 数据来源与真实性

`presentation.ts` 集中管理现有两个演示客户的中文展示别名（陈雨、王先生）和编号。
内部 customerId、认证映射、订单归属和写入参数均不改变。只有已知展示名和完整展示编号
会映射回原 ID 进行搜索。其他搜索沿用历史消息与客户 ID 查询；不声称全量 CRM/订单检索。
列表的「订单相关/退款相关」来自最近消息关键词，并在提示中说明，不代表模型识别的意图。

上下文读取既有 CustomerContextProvider 的客户资料和订单。当前 Provider 仍为 Mock，
页面标记「演示客户」。没有伪造联系方式、渠道、优先级、知识来源、情绪或置信度。

`AIActivity` 只依据执行记录中的工具结果和关联审批展示动作：

- 订单查询成功、无匹配订单或查询失败。
- 已创建的退款审批；不将提交审批描述为已退款。
- 已持久化客户回复、执行未完成或失败。

`workspace-data.ts` 是只读 UI 查询，按当前会话与首屏消息 ID（最多 50 条）读取实际执行记录。
它与上下文的执行记录分页分开，避免历史执行超过 50 条时首屏新消息失去对应动作。
这不是模型内部推理展示，也不是模拟的逐步动画。

## 组件

保留 WorkspaceLayout、ConversationList、ConversationHeader、MessageHistory、MessageBubble、
MessageComposer、QuickReplies、AIStatus。新增 ContextPanel、AIActivity、WorkspaceIcon、
WorkspaceLoading 和展示数据工具。旧 CustomerDrawer 被可收起的上下文面板替代。
状态管理仍在原 MessageHistory 中；`workspace.css` 统一管理工作台 Token 与响应式布局。

## 验证

- `pnpm test`、`pnpm lint`、`pnpm build`。
- `PLAYWRIGHT_CHANNEL=msedge pnpm test:e2e:chat:https`（PowerShell 分开设置环境变量）。
- 原订单、审批批准/拒绝、人工回复、失败重试、客户续聊、关闭、账号隔离等完整流程。
- 1440 / 1280 / 1024，三种面板的 8 种组合，中央宽度随收起增加，输入区可见、布局不溢出。
- 刷新恢复布局，中文展示名搜索、无结果与清除，长消息/草稿、消息阅读位置。
- AI 展示不编造无依据步骤，退款申请关联到正确执行，UI 读取保持会话范围与分页边界。
- 截图：忽略目录 `.next/acceptance/workspace-v2-*.png`。

## 保留问题与后续建议

1. AI 处理记录目前是首屏消息的持久化快照；继续加载的更早消息和实时新执行没有独立的
   前端事件流。完整实时进度/意图/知识来源需要稳定的执行事件读取协议，应单独设计。
2. 人工接管时消息与发送权限随现有轮询更新；Header、队列、上下文仍是服务端快照。
   多坐席同步、未读与「我的会话」需要先确定分配/已读语义，不能用 UI 推断。
3. 当前没有「AI 生成回复草稿」服务。需要后续设计只生成草稿、人工确认发送的接口，
   不应调用现有会自动回复客户的 Runtime 来假装生成建议。

本次停在第二版 UI 验收，不继续开发其他模块。
