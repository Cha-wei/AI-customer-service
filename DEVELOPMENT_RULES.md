# Development Rules

本文档规定 Codex 在本项目中的开发协作方式。

目标：
- 方便代码追踪
- 方便回滚
- 避免无关修改
- 保持代码可维护
- 降低后续理解和修改成本

## 1. 开发原则
- 优先完成当前任务。
- 不主动扩大任务范围。
- 不为了未来可能使用的功能提前实现复杂架构。
- 不在没有必要的情况下重构正常工作的模块。
- 优先实现最小、完整、可验证的方案。

## 2. 分阶段开发
较大的任务应拆成多个有意义的阶段。

每个阶段应该：
1. 有明确目标
2. 完成后可以运行或验证
3. 不留下明显半完成状态
4. 尽量对应一个独立 Git Commit

## 3. 阶段验证、Git Commit 与 GitHub Push
这是产生文件修改任务的唯一收尾流程。完成一个有意义阶段后执行一次；只读任务不适用，用户明确要求暂不提交或推送时遵循该限制。

1. 更新与此次修改直接相关的必要文档。
2. 检查 git status 和 git diff，确认修改范围；不纳入他人未提交修改、凭证、token、密码、本地数据库或敏感配置。
3. 按第 4 节运行相关验证，修复此次修改引入的问题；存在未解决失败时说明原因，不宣称验证通过。
4. 只暂存本次文件，检查暂存差异后创建一个有意义的 Commit。
5. 推送到当前分支对应的已配置 GitHub 远程分支。正常提交和推送已获项目授权，不重复询问；敏感操作按第 11 节处理。

Commit 示例：

```text
feat: add conversation model
feat: implement mock order tool
feat: add policy approval flow
test: add tool execution tests
fix: handle failed approval execution
```

一个 Commit 应对应一个清晰功能或修改。

不要：
- 把大量无关修改放进同一个 Commit
- 为极小的机械修改创建大量无意义 Commit
- 在测试失败或代码明显不可运行时直接提交

如果当前环境无法 Push、没有远程仓库、没有权限、认证失败或 Push 存在风险，应明确报告原因，不要假装已经 Push 成功。

## 4. 验证范围与结果复用
- 纯文档修改：检查内容一致性、引用路径、格式和 git diff --check；无需运行应用测试或构建。文档内可执行命令变化时验证受影响命令。
- 代码行为修改：运行受影响模块的测试及必要的类型、静态检查；涉及构建配置、依赖或生产运行路径时验证构建。
- 认证、权限、审批、数据隔离及跨模块核心流程变化：保留相关集成测试，必要时运行对应端到端验收。
- UI 变更的视觉验证范围见 UI_GUIDELINES.md；具体测试命令按需查 TESTING.md。
- 同一代码及相关配置、依赖、环境未变化时，复用本阶段已通过的检查；仅在相关修改、新失败或未解决疑点出现时重跑或扩大验证。
- “检查”“确认”默认指代理自行验证，不表示向用户申请批准。无法完成必要验证时明确报告限制。

## 5. 修改范围
优先只修改当前任务相关文件。

如果需要修改其他模块：
- 判断是否属于当前任务必要修改
- 如果不是必要修改，不要顺手重构

## 6. 代码注释
不要给每行代码写注释。

以下情况应考虑添加注释：
- 复杂逻辑
- 架构边界
- 非明显设计决策
- 重要编排逻辑
- MVP 临时实现
- 兼容或限制条件

注释优先解释 WHY，而不是重复代码已经表达的 WHAT。

例如：

```text
// MVP currently assumes one active order per customer.
// Keep this lookup isolated so it can later be replaced by CRM integration.
```

## 7. 文件与模块结构
模块应保持职责明确。

如果单个文件明显承担多个独立职责，可以拆分。

不要为了形式整洁而过度拆文件。

## 8. 接口优先
未来可能替换的模块优先定义稳定接口，例如：
- Knowledge Provider
- Customer Repository
- Tool
- Policy Engine

Mock 和真实实现应尽量通过相同接口切换。

## 9. 测试
测试重点覆盖：
- 核心业务流程
- 模块接口
- Policy 判断
- Tool 调用
- Approval 流程
- 错误处理

不要为了覆盖率数字编写大量没有实际价值的测试。

## 10. Bug 修复
修复 Bug 时：
1. 定位问题
2. 找到根因
3. 修改最小必要范围
4. 行为缺陷优先补充回归测试；已有测试足够或仅修正文案、轻微样式时，执行针对性验证，不编写低价值测试
5. 避免顺手进行无关重构

## 11. Codex 执行原则
需求足够明确时直接执行。

不要因为微小实现选择频繁询问。

在遵守上层指令的前提下，以用户当前明确要求确定任务范围。文档读取统一遵循 AGENTS.md；代码和测试用于核实现状，不把文档列表当作固定读取顺序。

直接执行或说明后继续：
- 已授权范围内的低风险、可逆修改，以及必要的相关模块调整。
- 常规实现选择、只读检查、相关测试和第 3 节约定的正常提交推送。
- 用户已明确授权的架构调整；说明影响，不仅因其属于架构变化而重复询问。

仅在缺少关键决定或必要授权时暂停对应操作，并简要说明具体原因：
- 需求存在无法由当前明确要求消解的冲突，或重大架构选择将扩大任务范围。
- 未授权的不可逆数据修改、破坏性删除、生产环境变更、权限扩大或敏感数据对外传输。
- 强制推送、改写共享历史、删除远程分支，或无法确定推送目标。
- 必要权限、凭证或信息缺失，无法继续对应步骤。

已有明确授权不重复索取；操作范围或风险实质变化时再确认。继续完成不受阻塞影响的工作。
发现可能泄露凭证时先移除或脱敏，不把普通提交理解为泄露授权；遵守不可绕过的安全限制。

## 12. 阶段结果说明
完成第 3 节后，简要总结实际改动、验证结果、提交与推送状态，以及未解决限制；不另起一轮检查。
仅在用户要求拆任务或存在明确独立后续工作时建议新任务；需要时提供启动 Prompt，不自动创建任务。
