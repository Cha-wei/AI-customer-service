# MVP 完整试用验收记录

日期：2026-09-13

## 结论

本机 HTTPS 演示范围内通过完整验收。客户与管理员使用独立浏览器上下文，
通过实际登录页面和业务按钮完成操作。订单、退款及客户资料使用 Mock 数据，
意图识别使用 rule；本次不调用外部模型、不产生真实退款。
远程部署、公网证书、真实业务接口不在本次结论内。

## 场景与结果

| 场景 | 结果 |
| --- | --- |
| 两个客户账号登录、账号身份隔离 | 通过 |
| 查询订单、刷新后恢复历史 | 通过 |
| 客户选择退款订单、等待审批 | 通过 |
| 管理员窗口点击批准退款，客户自动看到结果 | 通过 |
| 管理员窗口点击拒绝退款，客户自动看到结果 | 通过 |
| 客户请求人工、管理员筛选会话并回复 | 通过 |
| 客户继续留言、管理员接收更新并再次回复 | 通过 |
| 人工期间不新增 AI 执行或退款审批 | 通过 |
| 管理员关闭会话，双方禁止继续发送 | 通过 |
| 客户与客服断网发送后保留草稿、恢复后手动重试 | 通过 |
| 重复人工回复、跨客户消息、客户冒充客服、CSRF | 均被拒绝 |
| 长历史初次定位、新消息跟随、阅读旧消息保留位置 | 通过 |
| 账号切换清理旧消息/草稿、退出、重新登录 | 通过 |
| 过期 Cookie、停用/启用、密码重置后的旧会话 | 均被拒绝 |
| 登录限流、390px 客户布局、浏览器异常检查 | 通过 |

## 本轮发现与修复

1. 原有验收只检查消息存在于 DOM，未检查视区。增加长历史场景后，复现客户
   打开会话仍停在顶部的问题。修复后客户与管理员在底部时自动跟随消息；
   向上阅读时保留当前位置，使用“查看最新消息”返回底部。
2. 客户退出或认证失效后，侧栏仍显示“正在加载客户会话”。现明确提示重新登录，
   普通加载失败也显示对应提示。
3. 退款原通过 HTTP 表单请求验收，本轮改成管理员窗口实际点击批准/拒绝。
   新增客服发送断网保留草稿及手动重试的浏览器验收。

## 复现方式

```powershell
pnpm test
pnpm typecheck
pnpm lint
pnpm build
$env:PLAYWRIGHT_CHANNEL = 'msedge'
pnpm test:e2e:chat:https
```

210 项自动化测试通过，1 项可选外部模型测试跳过；类型检查、Lint、生产构建通过。
HTTPS 使用临时证书和隔离 SQLite 数据库，验收结束后清理；未改动日常业务数据。
脚本在被 Git 忽略的 `.next/acceptance/` 留下 customer.png 和 admin.png，
本轮已逐张查看，确认消息、角色和关闭状态显示正常。这些截图仅包含合成验收数据。

## 手动演示路径

1. 运行 `pnpm dev`，客户打开 `/chat/login`，管理员用独立浏览器窗口打开 `/login`。
2. 客户使用本地已配置账号，管理员使用本地管理密码。凭据不写入本记录或 Git。
3. 按上表顺序演示；退款为每个订单一次性的 Mock 操作，重复审批不会再次退款。
4. 若需要从空数据重演全部退款路径，运行隔离 HTTPS 验收脚本，不删除日常数据库。

本阶段保留单实例、共享管理员和一条客户消息待回复的 MVP 边界，未扩展新业务功能。
