# AGENTS.md

本文件定义 Codex 在本仓库中的工作方式。

## Project Goal
本项目用于实现 AI Customer Service Workbench 的 MVP。

系统应保持：
- 模块化
- 简单
- 可测试
- 可替换
- 易于扩展

MVP 阶段不要引入不必要的企业级复杂度。

## 文档读取规则
按任务影响范围读取相关章节，不因任务规模大而固定通读全部文档。
本会话已读取且未变化的内容直接复用；上下文缺失或文件变化时再补读。

- 小型实现：优先检查相关模块、接口和附近测试。
- 修改客服流程、审批行为或 Conversation 生命周期：读取 PRD.md 对应章节。
- 修改模块边界、Runtime 编排或 Provider / Adapter 接口：读取 ARCHITECTURE.md 对应章节。
- 涉及开发流程、验证或提交策略：读取 DEVELOPMENT_RULES.md 对应章节。
- UI 修改：按需读取 UI_GUIDELINES.md；选择测试命令时按需查 TESTING.md。
- 历史验收记录仅在追溯结果或执行对应验收时读取，不作为每次开发的前置条件。
- 只读审查和问答仅读取完成当前分析所需的材料。

跨多个领域的任务按上述条件组合读取，不另设全量文档清单。

## 执行与确认
在遵守上层指令的前提下，以用户当前明确要求确定范围；历史文档不扩大当前授权。
需求明确的低风险、可逆操作直接执行，常规实现选择和自行检查不需要用户确认。
已有授权持续有效；缺少关键决定或敏感操作授权时，按 DEVELOPMENT_RULES.md 第 11 节处理。

## 开发流程
理解目标、检查相关内容后，实现最小完整方案。
产生文件修改的任务统一按 DEVELOPMENT_RULES.md 第 3 节完成验证、提交与推送，不重复执行收尾流程。
只读任务只交付分析；用户要求暂不修改、提交或推送时，遵循该限制。
